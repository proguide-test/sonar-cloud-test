image: node:current-alpine

test:
  script:
    - npm install
    - npm run test
  only:
    - master
    - develop
    - merge_requests

upload:
  artifacts:
    paths: 
    - coverage/
  only:
    - master
    - develop
    - merge_requests
