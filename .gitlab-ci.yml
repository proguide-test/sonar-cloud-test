image: node:current-alpine

variables:
  DEBUG: "true"

test:
  script:
    - npm install
    - npm run test
  artifacts:
    paths: 
     - 'coverage/lcov-report/index.html'
  only:
    - master
    - develop
    - merge_requests
