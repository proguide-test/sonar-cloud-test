image: node:current-alpine

test:
  script:
    - npm install
    - npm run test
  artifacts:
    ignore_failure: false
    reports:
      junit: test_report.xml
  only:
    - master
    - develop
    - merge_requests
