image: node:current-alpine

test:
  script:
    - npm install
    - npm run test
  artifacts:
    expose_as: 'Cobertura'
    paths: ['coverage/lcov-report/index.html']
  only:
    - master
    - develop
    - merge_requests
