image: node:current-alpine

test:
  variables:
    DEBUG: true
  script:
    - npm install
    - npm run test
  artifacts:
    expose_as: 'Cobertura'
    paths: ['coverage']
  only:
    - master
    - develop
    - merge_requests
