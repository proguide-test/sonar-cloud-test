image: node:current-alpine

variables:
  NO_PROXY: "gitlab.proguidemc.com"

test:
  script:
    - npm install
    - npm run test
  artifacts:
    expose_as: 'Cobertura'
    paths: ['coverage']
  only:
    - master
    - develop
    - merge_requests
