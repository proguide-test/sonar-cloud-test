const fileNameHeader = getFileName(prefix + "_header.html");      
const fileNameFooter = getFileName(prefix + "_footer.html");
const fileNameDefHeader = getFileName("default_header.html");
const fileNameDefFooter = getFileName("default_footer.html");

if (fs.existsSync(fileNameHeader)) {
cmd = replaceAll(cmd, ["--header-html [header]"], [""]);            
cmd += ` --header-html "${fileNameHeader}"`;
} 

if (fs.existsSync(fileNameFooter)) {
cmd = replaceAll(cmd, ["--footer-html [footer]"], [""]);            
cmd += ` --footer-html "${fileNameFooter}"`;
}

cmd = replaceAll(cmd, ["[header]", "[footer]"], [fileNameDefHeader, fileNameDefFooter]);            
cmd += ` "${fileNameHTML}" "${fileNamePDF}"`;
